spring:
  datasource:
    url: jdbc:mysql://localhost:3306/concesionario?e&serverTimezone=UTC
    data-username: root 
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        ddl-auto: update
  jackson:    
    default-property-inclusion: non-null
